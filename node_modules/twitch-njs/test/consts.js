const should = require('chai').should();
const consts = require('../consts');

describe('#consts', function () {
    it('has constants defined', function () {
        should.exist(consts);
    });

    it('has the API URL set', function () {
        should.exist(consts.APIURL);
    });
});